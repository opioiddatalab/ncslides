# renv library (restored from renv.lock)
renv/library/
renv/staging/
.Rproj.user/
.Rhistory
.RData
.Ruserdata

# local scratch
# throwaway deck builds: DECK_OUT=build Rscript r/build_nightly.R
# (docs/ itself is tracked -- the nightly job publishes it)
build/
logs/
*.log
.DS_Store
